// VARIABLE => vary + able = Chang & Capable = Changeable

var weight = 38;
var temperature = 38;
var people = 38;

console.log(temperature);

// let, const (block-scope)
// let can be reassigned, not hoisted
let price = 100;
price = 50;
price = 200;

// const can not be reassigned, Assign at declaration
const pi = 3.14;

// Five Parts of Declaring a Variable
let studentName = "Alice";

/**
 * 1. keyword (let) = Tells JS you're creating a variable
 * 2. Variable Name (studentName) = A unique identifier you choose - use camelCase
 * 3. Assignment Operator (=) = Puts the value into the variable
 * 4. value ("Alice") = The data you want to store
 * 5. Semicolon (;) = Ends the statement
 */

/**
 *
 * Variable Declaration - Declaring a variable means reserving (সংরক্ষণ) a space in memory - like getting an empty labeled box, before putting anything in it.
 * 1. Write a keyword: let or const
 * 2. Give it a descriptive named in camelCase
 * 3. Value is undefined until assigned
 *
 */

// Printing Variable = use console.log()to display the value of a variable in the browser console

// Using before declaring is common mistake

// Variables store data = declare, then assign, consol.log() to check
